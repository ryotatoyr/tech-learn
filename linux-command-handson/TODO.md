# ハンズオン進捗管理 (TODO)

## Phase 1: 基本操作とファイル権限
- [ ] カレントディレクトリの確認と、検証用フォルダ `practice-dir` の作成
- [ ] 任意のファイルの作成 (`touch`, `echo`) とパーミッション (`chmod`)・所有者 (`chown`) の変更
- [ ] `ls -l` を実行し、パーミッション表記（rwxr-xr-x など）の意味を読み解く
- [ ] `sudo` による管理者特権コマンドの実行を体験する

## Phase 2: パッケージ管理と環境構築
- [ ] Debianパッケージインデックスの更新 (`sudo apt update`)
- [ ] `curl` や `wget` など、基本的な通信ツールがインストールされているか確認し、無ければ導入する
- [ ] 気象データ解析ツール群のインストール：
  - [ ] NetCDF 操作ツール (`sudo apt install netcdf-bin`)
  - [ ] CDO (`sudo apt install cdo`)
  - [ ] NCO (`sudo apt install nco`)

## Phase 3: テキストストリーム処理と検索
- [ ] テキストファイル（例：`/etc/passwd` など）の一部（head, tail）の表示
- [ ] `grep` を用いた特定文字列の抽出
- [ ] `awk` または `cut` を用いて、テキストから特定の列（フィールド）を切り出す
- [ ] `sed` を用いた正規表現による置換処理の実行
- [ ] パイプ `|` を使い、複数のコマンド（例：`cat` -> `grep` -> `awk` -> `sort` -> `uniq`）を繋いでデータを集計する

## Phase 4: システムモニタリングとプロセス管理
- [ ] 動作中のプロセス一覧を確認する (`ps aux`, `top`)
- [ ] テスト用にバックグラウンド実行させたプロセスを `kill` で終了する
- [ ] ディスク残量 (`df -h`) やメモリ空き容量 (`free -m`) を確認する

## Phase 5: ネットワークとファイル転送
- [ ] `ping` を使って特定のサーバーとの通信疎通を確認する
- [ ] `curl` や `wget` を用いて、インターネット上の公開データ（気象関連データやCSV）をダウンロードする

## Phase 6: 気象データ解析用の特殊コマンド
- [ ] **NetCDF操作**:
  - [ ] サンプルのNetCDFファイル (`.nc`) を準備するかダウンロードする
  - [ ] `ncdump -h` を実行し、次元 (dimensions) や変数 (variables)、グローバル属性を確認する
- [ ] **NCO操作**:
  - [ ] `ncks` を用いて、NetCDFファイルから特定の変数のみを切り出す
- [ ] **CDO操作**:
  - [ ] `cdo sinfo` でファイル情報を確認する
  - [ ] `cdo timmean` を用いて、時間平均値を出力する
- [ ] **GRIB2操作**:
  - [ ] `wgrib2` を用いて、GRIB2ファイルのメタデータをダンプする（※ Debian公式リポジトリに wgrib2 が見当たらない場合は、手動コンパイルするか、CDO経由での読み込みを検証してください）

## Phase 7: シェルスクリプト基礎
- [ ] 空の `run.sh` を作成し、先頭にシバン (`#!/bin/bash`) を記述する
- [ ] スクリプトに実行権限を付与する (`chmod +x run.sh`)
- [ ] 引数を受け取るスクリプトを記述する
- [ ] 気象データのダウンロード・NetCDF抽出・CDOによる平均値計算を自動で行う一連のシェルスクリプトを完成させる
