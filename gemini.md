# Gemini CLI向けプロジェクトコマンド

この`gemini.md`ファイルは、`tech-learn`プロジェクト、特に`secure-arch-handson`サブディレクトリに関するコンテキストと一般的なコマンドを提供します。

## `secure-arch-handson`プロジェクト

`secure-arch-handson`ディレクトリにはTypeScript Node.jsプロジェクトが含まれています。

### 依存関係のインストール

プロジェクトの依存関係をインストールするには：
```bash
npm install --prefix secure-arch-handson
```

### プロジェクトのビルド

TypeScriptコードをJavaScriptにコンパイルするには：
```bash
npm run build --prefix secure-arch-handson
```

### サーバーの実行

Node.jsサーバーを起動するには：
```bash
npm start --prefix secure-arch-handson
```

### 学習コンテンツの要約（今後）

このプロジェクトは学習教材です。進行に応じて、`secure-arch-handson/learning-plan.md`または他の学習リソースからコンテンツを要約するようにGeminiに依頼できます。
たとえば、`learning-plan.md`の「ステップ」または「フェーズ」を完了した後、次のように尋ねることができます。
「`secure-arch-handson/learning-plan.md`の『セキュアなアーキテクチャ入門』セクションの主要な要点を要約してください。」

または、特定のステップに関連するコミット間の変更の要約を依頼することもできます。
例：「`secure-arch-handson`ディレクトリに関連する過去3つのコミットで行われた変更を要約してください。」

### プロジェクトの進捗状況
プロジェクトの進捗は、`secure-arch-handson/TODO.md`ファイルで管理されます。Geminiはこのファイルを参照して、学習者が次に何をすべきかを案内できます。

### 開発のTipsとナレッジ
開発中に得られた環境固有のトラブルシューティングや便利なコマンドは、`docs/knowledge-base.md` に蓄積されています。問題が発生した際や手順を確認する際に参照してください。

---

## プロジェクトの規約

### Markdownファイルの言語

このプロジェクト内のすべてのMarkdownファイルは、**日本語**で記述してください。

### 学習の補助

phaseを進める等の行為は、学習者が主体的に行うべきです。Geminiはあくまで補助的な役割を果たします。変更やコマンドの実行、ターミナル上での動作チェックは、**Geminiは実行せず**、学習者が手動で行うようにさせてください。
また、学習者が理解できるように、必要に応じて手順や説明、簡易的なコード例を提供してください。

### エラーの修正
動作チェックでエラーが発生した場合、学習者がエラーメッセージを提供した上で、Geminiに修正方法を尋ねるようにしてください。
Geminiはエラーメッセージをもとに、修正手順やコード例を提供します。学習者はそれを参考にして、自分で修正を行います。

### 進捗管理
 
進捗管理は各ディレクトリにある`TODO.md`で行います。Geminiはこのファイルを参照し、学習者が次に何をすべきかを案内できます。進捗が発生したら、随時このファイルを確認し、**geminiが**更新してください。
このタイミングで、変更の差分をgitにcommitしてください。
また、それが3つ以上たまったら、変更の差分をまとめてgitにpushしてください。

### 要約の作成
学習者が特定のセクションやステップを完了した後、Geminiに要約を依頼できます。作成した要約は、`summaries/`ディレクトリにMarkdownファイルとして保存してください。ファイル名は、要約したセクションやステップに関連する名前にしてください（例：`phase-1-summary.md`）。
要約は以下のフォーマットに統一してください：

1. **タイトル**: `# Phase X 要約：[フェーズ名]`
2. **導入**: フェーズの目的を1-2文で簡潔に記載します。
3. **学んだこと**: 主要なトピックごとに以下の3項目を記述します。
    - **概要**: 何を目的としたタスクか
    - **実装**: 具体的にどのようなツールや手法を用いたか
    - **効果**: それによってどのような改善や成果があったか
4. **初めて登場した用語・概念**: `## 初めて登場した用語・概念` セクションに、新しく登場した用語、技術、ライブラリ等の簡潔な説明を記載します。

### 継続的な改善とナレッジの蓄積
タスクの完了（Phaseの終了や大きな修正の完了など）の際、Geminiは一連のやり取りを振り返り、今後役立つ情報（特定の環境での注意点、便利なコマンド、繰り返し発生した問題の解決策など）があるかを確認してください。もしあれば、`gemini.md` や関連ファイルへの追記・更新を学習者に提案してください。
